<UserControl x:Class="StoreClient.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StoreClient.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="7*"/>
            <ColumnDefinition Width="25*"/>
        </Grid.ColumnDefinitions>
        <Grid Background="LightGray" Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="19*"/>
                <RowDefinition Height="19*"/>
                <RowDefinition Height="19*"/>
                <RowDefinition Height="112*"/>
            </Grid.RowDefinitions>
            <Button Grid.Row="0" Content="Magazyn" Margin="3,5,3,5" Click="StorageButton_Click"></Button>
            <Button Grid.Row="1" Content="Zamówienia" Margin="3,5,3,5" Click="OrdersButton_Click"></Button>
            <Button Grid.Row="2" Content="Użytkownicy" Margin="3,5,3,5" Click="UsersButton_Click"></Button>
        </Grid>
        <ContentControl x:Name="PagableContent"  Grid.Column="1"/>
    </Grid>
</UserControl>
